import { createAction } from '@ngrx/store';

export const loadWelcomeScreen = createAction('[Layout] Load Welcome');

export const loadLogScreen = createAction('[Layout] Load log screen');

export const loadConfigurationScreen = createAction('[Layout] Load configuration screen');
